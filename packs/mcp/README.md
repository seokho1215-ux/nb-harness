# MCP Pack

**Unique close-proof:** an MCP tool-connection change is "done" only when the **wiring actually works and its
permissions are sane** — proven by captured connection/validation output. The Core **`mcp` category floor**
fires on `mcp.json`/`.mcp` changes; granting new scopes needs approval (tool-connection + risky permissions =
the unique proof that earns MCP pack status).

`close_contract`: objective `mcp-validate` (standard). Activates on mcp config paths and `mcp` commands.

> Status: `scaffold` — contract designed, not yet exercised end-to-end.
