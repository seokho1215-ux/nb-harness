# Evidence — <task>

> Step 2 of 5. Fill in AFTER the work. Maps to Core rule `evidence_ledger`.
> Gate: "done" requires the REAL verify output below, not a claim that it works.

## What was implemented
<short description + the files touched>

## Verify command
```
<the exact command you ran — build / test / lint>
```

## Verify output (paste the real output, not a summary)
```
<paste stdout/stderr here — pass/fail counts, errors, everything>
```

## Result
- [ ] verify command passed
- [ ] output is pasted above (not paraphrased)
