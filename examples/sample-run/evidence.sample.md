# Evidence — settings-name

## Verify command
`npm run build && npm test`

```
> build
✓ compiled successfully

> test
✓ saves display name
✓ persists across reload
✓ trims names over 80 chars
12 passed
```

Shown, not claimed — build green, 12 tests pass. This is what Gate B requires before "done".
